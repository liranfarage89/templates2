version: 2
deploy:
  steps:
    cfCreateChangeSet:
      before:
        - name: Enforce Networking Policies  
          run: |
            echo "ENV0_CF_PARAM_Comment=KMAC" >> $ENV0_ENV
            #cat $ENV0_CF_PARAMETERS_FILE
