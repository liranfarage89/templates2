---
version: 2
deploy:
  onCompletion:
    - name: On Completion
      run: echo omCompletion from parent
  steps:
    terraformPlan:
      after:
        - name: Security check using checkov
          run: echo parent

